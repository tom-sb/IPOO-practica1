#include <stdio.h>
#include <string.h>

void revert(char str[], int size)
{
    if (size!=EOF)
    {
        printf("%c",str[size]);
        revert(str,size-1);
    }
}
int main()
{
    char palabra[]="hola";
    revert(palabra,strlen(palabra));
    return 0;
}
