#include <stdio.h>
#include <string.h>

void revert(char str[], int n)
{
    if (n!=EOF)
    {
        printf("%c",str[n]);
        revert(str,n-1);
    }
    printf("\n");
}
int main()
{
    char palabra[]="hola";
    revert(palabra,strlen(palabra));
    return 0;
}
